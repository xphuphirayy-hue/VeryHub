--== Very Hub Loader (Obfuscated-Style) =========================================--
getgenv().VeryHubConfig = getgenv().VeryHubConfig or {Language = "TH"}

local _𝘃 = "V"              -- Key
local _𝘪𝘥 = 76558904092080  -- Required Place ID
local _𝘱𝘳𝘭 = game.PlaceId    -- Current Place ID

-- Language pack
local _𝘭𝘢𝘯𝗴 = {
    TH = {
        f9 = "โหลด -1000% ก่อนรันสคริป",
        keybad = "คีย์ไม่ถูกต้อง!",
        mapbad = "แผนที่ไม่ตรง! หยุดรัน",
        autostart = "เริ่มทำงานอัตโนมัติ Very Hub",
        farms = "ฟาร์มหิน",
        joinlow = "ย้ายเซิร์ฟคนน้อย...",
    },
    EN = {
        f9 = "Load -1000% before running script",
        keybad = "Invalid Key!",
        mapbad = "Invalid Map! Stopping.",
        autostart = "Auto-Starting Very Hub",
        farms = "Rock Farming",
        joinlow = "Joining low-player server...",
    },
    CN = {
        f9 = "在运行脚本前先加载 -1000%",
        keybad = "密钥错误!",
        mapbad = "地图不匹配!",
        autostart = "自动启动 Very Hub",
        farms = "挖石头",
        joinlow = "加入低人数服务器...",
    },
    JP = {
        f9 = "スクリプト実行前に -1000% をロードしてください",
        keybad = "キーが無効です！",
        mapbad = "マップが違います！停止します。",
        autostart = "Very Hub 自動起動",
        farms = "岩ファーム",
        joinlow = "人数少ないサーバーへ移動中…",
    }
}

local L = _𝘭𝘢𝘯𝗴[getgenv().VeryHubConfig.Language] or _𝘭𝘢𝘯𝗴.TH

warn(L.f9)

-- Key System
local function _𝘬(_v)
    return _v == _𝘃
end

if not _𝘬(_𝘃) then
    game.Players.LocalPlayer:Kick(L.keybad)
    return
end

-- Map Check
if _𝘱𝘳𝘭 ~= _𝘪𝘥 then
    game.Players.LocalPlayer:Kick(L.mapbad)
    return
end

warn(L.autostart)

--== Safe Wait
local function _𝘸(_t)
    task.wait(_t or 0)
end

--== Noclip
task.spawn(function()
    while true do
        _𝘸()
        local c = game.Players.LocalPlayer.Character
        if c then
            for _,v in pairs(c:GetChildren()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
        end
    end
end)

--== Fly Move
local function _𝗳𝗹𝗬(_to, _spd)
    local h = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
    local bv = Instance.new("BodyVelocity", h)
    bv.MaxForce = Vector3.new(9e9,9e9,9e9)
    while h and (h.Position - _to.Position).Magnitude > 6 do
        bv.Velocity = (_to.Position - h.Position).Unit * _spd
        _𝘸()
    end
    bv:Destroy()
end

--== Wait Until Object Gone
local function _𝗐(_obj)
    while _obj and _obj.Parent do _𝘸(0.1) end
end

--== Rock Farm Core
local function _𝗙𝗮𝗿𝗺()
    local _r = workspace:WaitForChild("Rocks")
    
    for _, folder in ipairs(_r:GetChildren()) do
        for _, obj in ipairs(folder:GetChildren()) do
            if obj:IsA("Model") then
                for _, part in ipairs(obj:GetChildren()) do
                    if part:IsA("Model") and part:FindFirstChild("Hitbox") then
                        local hb = part:FindFirstChild("Hitbox")
                        if hb then
                            _𝗳𝗹𝗬(hb, 20)
                            _𝗐(part)
                        end
                    end
                end
            end
        end
    end
end

--== Server Hop (<5 players)
local function _𝗷𝗹𝗼𝘄()
    warn(L.joinlow)
    local ts = game:GetService("TeleportService")
    local servers = game.HttpService:JSONDecode(
        game:HttpGet("https://games.roblox.com/v1/games/".._𝘪𝘥.."/servers/Public?sortOrder=Asc&limit=100")
    )

    for _, s in ipairs(servers.data) do
        if s.playing < 5 then
            ts:TeleportToPlaceInstance(_𝘪𝘥, s.id)
            return
        end
    end
end

--== UI Load (Kavo)
local K = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local W = K.CreateLib("Very Hub", "BloodTheme")
local Tab = W:NewTab("Very Hub")
local Sec = Tab:NewSection(L.farms)

Sec:NewButton("เริ่มฟาร์ม", "Start Rock Farming", function()
    task.spawn(function()
        while true do
            _𝗙𝗮𝗿𝗺()
            _𝘸()
        end
    end)
end)

Sec:NewButton("ไปเซิร์ฟคนน้อย", "Join Server <5 Players", function()
    _𝗷𝗹𝗼𝘄()
end)

